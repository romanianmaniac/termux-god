# Скрипты запускаются в Termux (Linux) — переносы строк ВСЕГДА LF,
# иначе на телефоне: bad interpreter: /usr/bin/env bash^M
* text=auto eol=lf
*.sh   text eol=lf
install.sh text eol=lf
termux-god text eol=lf
